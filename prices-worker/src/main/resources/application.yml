spring:
  datasource:
    url: jdbc:h2:file:${DATABASE_FILE}
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        format_sql: true

import:
  csv: ${CSV_FILE}
